@{
    ViewData["Title"] = "Add QuestionType";
    Layout = "~/Views/shared/_Layout.cshtml";
}

@model InterviewQuestionPortal.Areas.QuestionType.Models.QuestionTypeModel

<main class="main" id="main">
    <div class="row">
        <!-- Basic Layout -->
        <div class="row">
            <div class="col-xl">
                <div class="card mb-4">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">Add QuestionType</h5>
                        @*<small class="text-muted float-end">Default label</small>*@
                    </div>
                    <div class="card-body">
                        <form>
                            @Html.HiddenFor(x=>x.QuestionTypeID)
                            @*<div class="form-floating form-floating-outline mb-4">
                            <input readonly type="text" asp-for="SubjectID" class="form-control" id="basic-default-fullname" placeholder="Enter SubjectName" />
                            <label for="basic-default-fullname">SubjectID</label>
                            </div>*@
                            <div class="form-floating form-floating-outline mb-4">
                                <input type="text" asp-for="QuestionType" class="form-control" id="basic-default-fullname" placeholder="Enter SubjectName" />
                                <label for="basic-default-fullname">QuestionType</label>
                            </div>

                            <button type="submit" class="btn btn-primary" asp-area="QuestionType" asp-controller="QuestionType" asp-action="Save">Submit</button>
                            <button type="submit" class="btn btn-primary" asp-area="QuestionType" asp-controller="QuestionType" asp-action="QuestionTypeList">Back</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>
@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}